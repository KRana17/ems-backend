version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://db:3306/new
      SPRING_DATASOURCE_USERNAME: ems_usr
      SPRING_DATASOURCE_PASSWORD: Federer07
    depends_on:
      - db

  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: Sqlrockstar
      MYSQL_DATABASE: ems_db
      MYSQL_USER: ems_usr
      MYSQL_PASSWORD: Federer07
    ports:
      - "3309:3306"
